const DataConsumerV3 = artifacts.require("DataConsumerV3");

contract("DataConsumerV3", (accounts) => {
  // TODO update with tests
  // mock data to look like ETH/USD or BTC-USD 30-Day Realized Volatility expected to be in location of the array


  // it("should increment value by 1", async () => {
  //   // Deploy the contract
  //   const backup = await DataConsumerV3.new();

  //   // Call the countAggregators method and get value
  //   const value = await backup.countDataAggregators();

  //   // Assert the value is as expected
  //   assert.equal(value.toString(), "3", "Number of data feeds not equal");
  // });
});